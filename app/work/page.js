export const metadata = {
  title: 'Works — GOMO',
};

export default function WorkPage() {
  return (
    <div className="page-container" style={{ paddingBlock: '48px' }}>
      <p>作品分類頁（骨架階段，篩選頁籤、卡片格線、動效之後搬進來）</p>
    </div>
  );
}
