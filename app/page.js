export default function HomePage() {
  return (
    <div className="page-container" style={{ paddingBlock: '48px' }}>
      <p>首頁（骨架階段，Loading → Hero → Section 2 → Gallery 動效之後分批搬進來）</p>
    </div>
  );
}
